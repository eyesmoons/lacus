spring:
  datasource:
      url: jdbc:mysql://127.0.0.1:3306/lacus?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false
      username: shengyu
      password: 123456
  redis:
    host: 127.0.0.1 # Redis服务器地址
    database: 2 # Redis数据库索引（默认为0）
    port: 16379 # Redis服务器连接端口
#    username: # Redis用户
#    password: # Redis服务器连接密码（默认为空）
    timeout: 10000ms # 连接超时时间（毫秒）


logging:
  level:
    root: info
    com.qtone.data.ds: debug

kafka:
  bootstrapServers: hadoop1:9092,hadoop2:9092,hadoop3:9092
